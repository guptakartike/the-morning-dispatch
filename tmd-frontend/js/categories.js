/**
 * categories.js — Category tab bar + footer categories (static placeholder)
 */

/** Shared cache — empty, no backend */
export let categoriesCache = [];

export function loadCategories() {
  // No backend connection — categories stay empty
}
